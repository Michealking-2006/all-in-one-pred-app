# Copy this to .env.local for local `vercel dev` testing (that file is gitignored).
# In production, set this in your hosting platform's environment variables instead —
# Vercel: Project Settings -> Environment Variables. Never commit the real value.
SCOUTWAVE_FOOTBALL_API_KEY=your_api_football_key_here
